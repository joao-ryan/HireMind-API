export type UserRole = "ADMIN" | "RECRUITER" | "CANDIDATE";

export interface IUser {
  _id: string;
  name: string;
  email: string;
  password: string;
  role: UserRole;
  createdAt: Date;
  updatedAt: Date;
}
